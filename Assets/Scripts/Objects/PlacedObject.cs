using UnityEngine;

// Empty marker component. ObjectPlacer sticks one of these on everything the user
// places, so later it can tell "this is a tree the user added" apart from "this is
// the ground". Nothing to implement - the component just needs to exist.
public class PlacedObject : MonoBehaviour
{
}

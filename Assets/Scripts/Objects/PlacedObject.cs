using UnityEngine;

// A tiny marker so the object placer knows which things in the scene were placed by
// the user (and can therefore be deleted). Added automatically to placed trees/rocks.
// Owner: Nabintou (object placement).
public class PlacedObject : MonoBehaviour
{
}
